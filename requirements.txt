# Pydantic AI handles structured LLM output end-to-end: provider abstraction,
# response_format/tool-use under the hood, automatic retry on validation
# failure (the model sees the validation error and corrects itself). Brings
# its own pinned versions of pydantic, anthropic, and openai.
pydantic-ai>=0.0.20
