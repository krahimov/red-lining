# Pick the SDK that matches your provided API key.
# OpenAI keys (sk-...) -> openai
# Anthropic keys (sk-ant-...) -> anthropic
openai>=1.40.0
anthropic>=0.34.0

# Pydantic gives us a single source of truth for the schema across the
# playbook input, the LLM output, and the evaluation inputs.
pydantic>=2.0
